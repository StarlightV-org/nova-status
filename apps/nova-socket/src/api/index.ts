import type { FastifyInstance } from "fastify";
import { getOnlineUsers, prankOverlay } from "~/api/onlineStatus";

// export async function onlineStatusRoutes(fastify: FastifyInstance) {
// 	fastify.get("/getOnlineUsers", getOnlineUsers);
// }
// onlineStatusRoutes.prefix = "/onlineStatus";
// export async function triggerRoutes(fastify: FastifyInstance) {
// 	fastify.post("/prank", prankOverlay);
// }
// triggerRoutes.prefix = "/trigger";
